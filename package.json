{
  "name": "animachina",
  "private": true,
  "version": "0.5.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "esbuild tests/runtime.test.ts --bundle --platform=node --format=esm --outfile=.test-build/tests.mjs && node --test .test-build/tests.mjs"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "@types/three": "^0.186.0",
    "typescript": "^5.9.2",
    "vite": "^7.1.7",
    "esbuild": "^0.25.0"
  }
}
